[tools]
java = "21.0.2"
maven = "3.9.9"

[env]
# Auto-load .env into the environment for any command run under mise.
# mise's dotenv parser handles special chars (e.g. & in the JDBC URL) that
# break `source .env` in the shell.
_.file = ".env"
