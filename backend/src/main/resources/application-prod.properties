# Production overrides — fail closed if required env vars are missing.
# Activate by setting SPRING_PROFILES_ACTIVE=prod (e.g. on Railway).
app.auth.username=${APP_AUTH_USERNAME}
app.auth.password=${APP_AUTH_PASSWORD}
