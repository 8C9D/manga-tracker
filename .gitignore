# Claude Code session data
.claude/

# Environment files — copy .env.example to .env and fill in values.
# Ignore every .env variant (.env.local, .env.production, .env.staging, ...)
# at any depth, but keep the committed placeholder template.
.env
.env.*
!.env.example

# macOS
.DS_Store

# Logs
*.log
/tmp/
